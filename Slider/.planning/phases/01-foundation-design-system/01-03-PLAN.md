---
phase: 01-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/page.tsx
  - components/ui/Button.tsx
  - components/ui/Card.tsx
autonomous: false

must_haves:
  truths:
    - "Page displays alternating dark and light sections that are visually distinct"
    - "Pill-shaped CTA buttons render with burnt-orange background and hover state transition"
    - "Rounded cards render with subtle borders/shadows on both dark and light backgrounds"
    - "Layout adapts responsively from desktop to tablet to mobile (no horizontal scroll, readable text)"
    - "Scrolling through sections demonstrates smooth scroll + design system working together"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with alternating dark/light section skeleton and responsive layout"
      contains: "bg-stone-950"
    - path: "components/ui/Button.tsx"
      provides: "Pill-shaped CTA button component with primary/secondary variants"
      contains: "rounded-button"
    - path: "components/ui/Card.tsx"
      provides: "Rounded card component with border and shadow"
      contains: "rounded-card"
  key_links:
    - from: "app/page.tsx"
      to: "components/ui/Button.tsx"
      via: "import and usage in hero/CTA placeholder sections"
      pattern: "import.*Button"
    - from: "app/page.tsx"
      to: "components/ui/Card.tsx"
      via: "import and usage in feature placeholder sections"
      pattern: "import.*Card"
    - from: "components/ui/Button.tsx"
      to: "app/globals.css"
      via: "Tailwind utilities consuming @theme tokens"
      pattern: "burnt-orange|rounded-button"
---

<objective>
Build the section skeleton with alternating dark/light backgrounds, reusable UI components (Button, Card), and responsive layout. This creates the visual foundation that all content sections will be built on.

Purpose: Prove the design system works end-to-end by rendering real components with design tokens, responsive layout, and alternating section backgrounds.
Output: Homepage with placeholder sections, Button and Card components ready for use in future phases.
</objective>

<execution_context>
@C:\Users\antoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\antoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-foundation-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button and Card UI components</name>
  <files>components/ui/Button.tsx, components/ui/Card.tsx</files>
  <action>
    Create `components/ui/Button.tsx`:
    - Pill-shaped CTA button using `rounded-button` (maps to 9999px from @theme)
    - Two variants: `primary` (burnt-orange bg, white text) and `secondary` (teal bg, white text)
    - Optional `outline` variant: transparent bg with burnt-orange border and text
    - Hover state transitions using `transition-colors duration-200`
    - Primary hover: `bg-burnt-orange-hover` (darker shade)
    - Two sizes: `default` (px-6 py-3) and `lg` (px-8 py-4 text-lg)
    - Accept standard button HTML attributes (onClick, type, disabled, etc.)
    - Use `font-medium` for button text weight

    ```tsx
    import { type ButtonHTMLAttributes } from 'react'

    interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: 'primary' | 'secondary' | 'outline'
      size?: 'default' | 'lg'
    }

    export default function Button({
      children,
      variant = 'primary',
      size = 'default',
      className = '',
      ...props
    }: ButtonProps) {
      const baseClasses = 'inline-flex items-center justify-center font-medium rounded-button transition-colors duration-200 cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed'

      const sizeClasses = {
        default: 'px-6 py-3 text-base',
        lg: 'px-8 py-4 text-lg',
      }

      const variantClasses = {
        primary: 'bg-burnt-orange hover:bg-burnt-orange-hover text-white',
        secondary: 'bg-teal hover:bg-teal-hover text-white',
        outline: 'border-2 border-burnt-orange text-burnt-orange hover:bg-burnt-orange hover:text-white',
      }

      return (
        <button
          className={`${baseClasses} ${sizeClasses[size]} ${variantClasses[variant]} ${className}`}
          {...props}
        >
          {children}
        </button>
      )
    }
    ```

    Create `components/ui/Card.tsx`:
    - Rounded corners using `rounded-card` (maps to 1rem from @theme)
    - Two themes: `light` (white/stone-50 bg, stone-200 border) and `dark` (stone-900 bg, stone-800 border)
    - Shadow using `shadow-card` token, with `hover:shadow-card-hover` transition
    - Padding: `p-6` default
    - Accept children and additional className

    ```tsx
    interface CardProps {
      children: React.ReactNode
      theme?: 'light' | 'dark'
      className?: string
      hover?: boolean
    }

    export default function Card({
      children,
      theme = 'light',
      className = '',
      hover = false,
    }: CardProps) {
      const baseClasses = 'rounded-card p-6 transition-shadow duration-200'

      const themeClasses = {
        light: 'bg-white border border-stone-200 shadow-card',
        dark: 'bg-stone-900 border border-stone-800 shadow-card',
      }

      const hoverClass = hover ? 'hover:shadow-card-hover' : ''

      return (
        <div className={`${baseClasses} ${themeClasses[theme]} ${hoverClass} ${className}`}>
          {children}
        </div>
      )
    }
    ```
  </action>
  <verify>
    Confirm both files exist and compile without TypeScript errors (check terminal for errors after saving).
    Both components should be importable from other files.
  </verify>
  <done>Button component renders pill-shaped CTAs with primary/secondary/outline variants and hover transitions. Card component renders rounded containers with light/dark themes and optional hover shadow.</done>
</task>

<task type="auto">
  <name>Task 2: Build homepage section skeleton with alternating backgrounds and responsive layout</name>
  <files>app/page.tsx</files>
  <action>
    Replace the default Next.js page with a section skeleton that demonstrates the full design system. Create 6 placeholder sections that mirror the final page structure, alternating dark and light backgrounds.

    Each section should:
    - Be a `<section>` element with an id for future nav linking
    - Use `min-h-screen` for full viewport height (or close to it)
    - Have centered content with max-width container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
    - Alternate between dark (stone-950/stone-900 bg) and light (stone-50/stone-100 bg) backgrounds
    - Include placeholder heading (using fluid typography classes) and brief description
    - Use responsive padding: `py-20 sm:py-24 lg:py-32`

    Section structure:
    1. **Hero** (dark bg: `bg-stone-950`) — Large heading with `text-display`, subline, Button (primary, lg), and Button (outline)
    2. **Features** (light bg: `bg-stone-50 text-stone-900`) — Section heading with `text-5xl`, 3 placeholder Cards (light theme) in a responsive grid (`grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`)
    3. **Product Demo** (dark bg: `bg-stone-900`) — Heading with `text-4xl`, placeholder demo area
    4. **Skills** (light bg: `bg-stone-100 text-stone-900`) — Heading, 4 Cards in grid
    5. **Use Cases** (dark bg: `bg-stone-950`) — Heading, 3 Cards (dark theme) in grid
    6. **CTA** (dark bg: `bg-stone-900`) — Heading with `text-4xl`, subline, Button (primary, lg)

    Responsive grid pattern for cards:
    ```
    grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
    ```

    For the hero section specifically, use flex layout for centering:
    ```
    min-h-screen flex flex-col items-center justify-center text-center
    ```

    Import and use both Button and Card components in appropriate sections to demonstrate they work with the design system.

    Placeholder text should be real-ish content about Slider (not lorem ipsum) so the page feels intentional even in skeleton form. Use headings like:
    - Hero: "Build Better Decks with AI"
    - Features: "Why Teams Choose Slider"
    - Demo: "See Slider in Action"
    - Skills: "AI-Powered Skills"
    - Use Cases: "Built for Every Team"
    - CTA: "Ready to Transform Your Presentations?"
  </action>
  <verify>
    Run `npm run dev` and open localhost:3000.
    Visually confirm:
    1. Alternating dark/light sections are clearly distinct
    2. Oversized headings render at different sizes (display > 5xl > 4xl)
    3. Buttons show pill shape with hover color transition
    4. Cards have rounded corners with visible border/shadow
    5. Resize browser to check responsive: cards stack on mobile, grid on desktop
    6. Smooth scroll is active when scrolling between sections
    7. No horizontal scroll overflow at any viewport width
  </verify>
  <done>Homepage displays 6 alternating dark/light sections with responsive grid layouts. Button and Card components render correctly using design tokens. Page is scrollable with smooth scroll active.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 foundation: Next.js 15 project with Tailwind CSS v4 design system (custom colors, fluid typography, shadows, radii), Lenis smooth scroll with GSAP ScrollTrigger integration (3-line bridge), reduced-motion support, Geist font, and a section skeleton with alternating dark/light backgrounds, pill-shaped Button components, and rounded Card components.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in browser
    2. Scroll through the page — should feel buttery smooth (Lenis active)
    3. Check headings scale fluidly: resize browser width from 400px to 1400px — text should grow/shrink smoothly
    4. Verify color palette: burnt-orange buttons, stone dark/light alternating sections, teal secondary elements
    5. Check responsive layout: at mobile width, card grids should stack to single column
    6. Hover over buttons — color should transition smoothly (burnt-orange darkens on hover)
    7. Hover over cards (if hover enabled) — shadow should lift
    8. Enable "Reduce motion" in OS accessibility settings — smooth scroll should become instant, CSS animations should be disabled
    9. Open DevTools > Elements > check that `<html>` has Geist font variable classes
    10. Check document title reads "Slider — AI Copilot for PowerPoint"
  </how-to-verify>
  <resume-signal>Type "approved" if the design system looks correct, or describe any issues with colors, typography, layout, or scroll behavior.</resume-signal>
</task>

</tasks>

<verification>
1. Button component: pill shape, burnt-orange primary, teal secondary, outline variant, hover transitions
2. Card component: rounded corners, light/dark themes, shadow, optional hover shadow
3. Homepage: 6 sections alternating dark (stone-950/900) and light (stone-50/100) backgrounds
4. Responsive: cards stack on mobile, grid on desktop, no horizontal overflow
5. Typography: display/5xl headings are visibly oversized, body text is readable
6. Smooth scroll active through all sections
7. Design tokens from globals.css @theme are consumed correctly by utility classes
</verification>

<success_criteria>
- Alternating dark/light sections render with clear visual contrast
- Pill-shaped buttons work with hover transitions
- Rounded cards render on both dark and light backgrounds
- Layout is responsive from mobile to desktop
- All design system tokens are consumed correctly
- Human approves visual quality of foundation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-system/01-03-SUMMARY.md`
</output>
