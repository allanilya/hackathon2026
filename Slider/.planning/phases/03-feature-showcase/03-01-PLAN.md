---
phase: 03-feature-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/FeatureSlide.tsx
autonomous: true

must_haves:
  truths:
    - "Each feature slide displays an icon and descriptive copy (title + description)"
    - "FeatureSlide component is reusable with typed props"
  artifacts:
    - path: "components/ui/FeatureSlide.tsx"
      provides: "Presentational feature slide component"
      exports: ["FeatureSlide"]
      min_lines: 20
  key_links:
    - from: "components/ui/FeatureSlide.tsx"
      to: "design system tokens"
      via: "Tailwind classes using project color tokens"
      pattern: "text-burnt-orange|text-stone"
---

<objective>
Create the FeatureSlide presentational component and define the feature content data array that will be consumed by the FeatureShowcase section.

Purpose: Establish the visual building block for each feature in the pinned scroll showcase -- icon, title, and description rendered in a centered layout with responsive sizing.
Output: A typed, reusable FeatureSlide component ready for horizontal scroll layout.
</objective>

<execution_context>
@C:\Users\antoi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\antoi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-showcase/03-RESEARCH.md
@components/ui/Card.tsx
@components/sections/Hero.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeatureSlide presentational component</name>
  <files>components/ui/FeatureSlide.tsx</files>
  <action>
Create `components/ui/FeatureSlide.tsx` as a presentational (non-client) component.

Props interface:
```typescript
interface FeatureSlideProps {
  icon: React.ReactNode   // SVG icon or illustration
  title: string           // Feature heading
  description: string     // Feature body text
}
```

Layout requirements:
- Outer div: `feature-slide` class (used by GSAP selector), `min-w-full h-full` for horizontal scroll layout, `flex flex-col items-center justify-center` for centering, `px-6 sm:px-8 lg:px-16` for responsive padding
- Add `motion-reduce:min-w-0 motion-reduce:min-h-[60vh] motion-reduce:py-16` for reduced motion vertical stacking fallback
- Inner content wrapper: `max-w-2xl text-center space-y-6`
- Icon container: `w-20 h-20 lg:w-28 lg:h-28 mx-auto text-burnt-orange` -- holds the ReactNode icon
- Title: `text-3xl lg:text-5xl font-bold text-white leading-tight`
- Description: `text-lg lg:text-xl text-stone-300 max-w-xl mx-auto`

Export as named export: `export function FeatureSlide(...)`.

This is a server-compatible component (no "use client" needed since it has no hooks or interactivity).
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Visually inspect file has correct structure with named export.
  </verify>
  <done>
FeatureSlide.tsx exists with typed props (icon: ReactNode, title: string, description: string), responsive Tailwind layout, reduced motion fallback classes, and named export.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define feature content data with inline SVG icons</name>
  <files>components/sections/featureData.ts</files>
  <action>
Create `components/sections/featureData.ts` to hold the feature content array separately from the animation component (keeps FeatureShowcase focused on animation logic).

Define a `Feature` type and export a `features` array with 4 feature objects. Each feature has:
- `id`: number (1-4)
- `title`: string
- `description`: string
- `iconPath`: string (SVG path data for a simple icon -- will be rendered as an SVG in FeatureShowcase)

Use these 4 features (content reflects Slider's value props):

1. **AI-Powered Skills** -- "Expert-crafted Skills that understand your intent and deliver professional results in seconds."
   Icon: A sparkle/wand shape (use a simple star/sparkle SVG path like `M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z` -- star shape)

2. **Lives in PowerPoint** -- "No context switching. Slider works directly in your familiar PowerPoint environment as a sidebar."
   Icon: A window/monitor shape (rectangle with inner line, e.g., `M4 4h16a2 2 0 012 2v12a2 2 0 01-2 2H4a2 2 0 01-2-2V6a2 2 0 012-2z` + `M2 10h20`)

3. **Instant Results** -- "Create polished slides in seconds, not hours. Focus on your message while AI handles the design."
   Icon: A lightning bolt/zap shape (e.g., `M13 2L3 14h9l-1 10l10-12h-9l1-10z`)

4. **Team Ready** -- "Share Skills across your organization. Everyone presents with the same professional quality."
   Icon: A people/users shape (e.g., `M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2` + `M9 11a4 4 0 100-8 4 4 0 000 8z` + `M23 21v-2a4 4 0 00-3-3.87` + `M16 3.13a4 4 0 010 7.75`)

Export:
```typescript
export interface Feature {
  id: number
  title: string
  description: string
  iconPaths: string[]  // Array of SVG path 'd' attributes
}

export const features: Feature[] = [...]
```

Using `iconPaths: string[]` (array of path d-attributes) keeps this file as a plain TS module with no JSX, keeping data separate from presentation.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no type errors. Verify file exports `features` array and `Feature` type.
  </verify>
  <done>
featureData.ts exists with typed Feature interface, 4 feature objects with titles/descriptions/iconPaths, exported as named exports.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `components/ui/FeatureSlide.tsx` exports `FeatureSlide` component with `icon`, `title`, `description` props
- `components/sections/featureData.ts` exports `features` array with 4 items and `Feature` type
- Both files use project design tokens (burnt-orange, stone colors, fluid typography)
</verification>

<success_criteria>
- FeatureSlide component renders centered icon + title + description with responsive sizing
- Feature content data defined with 4 Slider value propositions and SVG icon paths
- Both files have clean TypeScript types with no errors
- Reduced motion CSS fallback classes present on FeatureSlide
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-showcase/03-01-SUMMARY.md`
</output>
